
# Database Connection
# For Replit PostgreSQL: Copy from Database tab > env section
# For external providers (Neon, Supabase, etc.): Copy connection string
DATABASE_URL=postgresql://user:password@host:5432/database_name

# Session Secret
# IMPORTANT: Use a strong random string in production
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=replace-with-strong-random-secret-key-minimum-32-characters

# Server Configuration (optional)
# PORT=5000
# NODE_ENV=development
